package model.entities;

public class EasyAccount extends Accountant {

    public EasyAccount(int accountNumber, Agency agency, Client client) {
        super(accountNumber, agency, client);
    }
}
